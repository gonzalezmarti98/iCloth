<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project_iCloth">

        <activity
            android:name=".ClosetActivity"
            android:theme="@style/Theme.Project_iCloth" />

        <activity android:name=".SignUpActivity" />

        <!-- LoginActivity usa activity_main.xml y es la pantalla inicial -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.Project_iCloth">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity, sin intent-filter de momento -->
        <activity android:name=".MainActivity" />

    </application>

</manifest>
