<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:src="@drawable/bottom_navigation_bar"
        android:scaleType="fitXY"/>

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        app:itemIconSize="30dp"
        app:menu="@menu/items_nav_menu"
        app:itemIconTint="@color/nav_item_color"
        app:itemTextColor="@color/nav_item_color"
        app:labelVisibilityMode="unlabeled"
        app:itemBackground="@android:color/transparent"
        app:itemRippleColor="@color/nav_press_color">

    </com.google.android.material.bottomnavigation.BottomNavigationView>
</FrameLayout>
    <!--
    app:labelVisibilityMode="unlabeled" >>>>> con esto SOLO se ven los iconos, y alineados
    app:itemBackground="@android:color/transparent"> >>>>>>> desactiva indicador de selección (punto blanco)
    -->