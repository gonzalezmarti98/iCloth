<?xml version="1.0" encoding="utf-8"?>
<!-- res/layout/item_color_dot.xml -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="4dp">

    <!-- Este es el círculo de color -->
    <View
        android:id="@+id/colorDot"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@drawable/shape_color_dot_unselected" />

    <!-- Este es el 'tick' de selección que se mostrará u ocultará -->
    <ImageView
        android:id="@+id/ivSelectionTick"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_gravity="center"
        android:src="@drawable/ic_check_circle"
        android:visibility="gone"
        app:tint="@android:color/white" />

</FrameLayout>
